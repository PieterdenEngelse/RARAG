version: "1.0"

# Minimal Qodo agents configuration compatible with Qodo 0.30.2
# Adjust providers/models/keys later via UI or environment variables.

agents:
  - id: default
    name: Default Agent
    role: General purpose assistant
    description: Minimal default agent to enable Qodo Web UI startup.
    settings:
      provider: openai
      model: gpt-4o-mini
      temperature: 0.2
      max_tokens: 4096
    tools:
      - name: filesystem
        enabled: true
      - name: terminal
        enabled: true
      - name: browser
        enabled: true
    permissions:
      filesystem: read-write
      terminal: allow
      browser: allow
