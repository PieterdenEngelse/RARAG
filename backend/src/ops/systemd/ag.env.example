# Example environment for ag.service
# Copy to /etc/default/ag (Debian/Ubuntu) or /etc/sysconfig/ag (RHEL)

# Logging
RUST_LOG=info

# Network
BACKEND_HOST=127.0.0.1
BACKEND_PORT=3010

# Startup behavior
SKIP_INITIAL_INDEXING=true
TRUST_PROXY=false

# Rate limit config
# Use JSON or YAML (YAML requires building with --features rl_yaml)
RATE_LIMIT_ROUTES_FILE=/etc/ag/rl-routes.json
# Or provide inline JSON (array or {routes, exempt_prefixes})
# RATE_LIMIT_ROUTES='{"routes":[{"pattern":"/reindex","match_kind":"Exact","qps":0.5,"burst":2,"label":"admin-reindex"}]}'

# Redis (optional)
REDIS_ENABLED=true
REDIS_URL=redis://127.0.0.1:6379/
REDIS_TTL=3600
