#!/bin/bash
# ~/ag/tmuxr v1.0.1
# Restart both tmux sessions

# Kill existing sessions if they exist
tmux kill-session -t ba 2>/dev/null
tmux kill-session -t fo 2>/dev/null

# Run startup script
./tmux_startup.sh

echo "✅ Tmux sessions 'ba' and 'fo' restarted!"
